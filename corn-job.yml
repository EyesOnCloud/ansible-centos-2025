---
- name: Schedule a cron job for natasha
  hosts: cronservers
  become: yes
  tasks:
    - name: Schedule cron job for admin
      ansible.builtin.cron:
        name: "cron task"
        user: admin
        job: 'logger "Time: $(date)"'
        minute: "*/2"
