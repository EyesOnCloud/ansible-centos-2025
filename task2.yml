---
- name: Enable and start httpd service web servers
  hosts: webservers
  become: yes
  tasks:
    - name: Start service httpd, if not started
      ansible.builtin.service:
        name: httpd
        state: started
